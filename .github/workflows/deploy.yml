name: Deploy to FTP server

on:
  push:
    branches:
      - master

jobs:
  ftp-deploy:
    name: FTP Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # Собирает dist/: форму входа (server/*.php), приложение в app/ и
      # статику в assets/*.dat. Подробности — в комментарии build.mjs.
      - name: Build dist
        run: node build.mjs
        env:
          # Ключ Comtrade Plus (необязательный). Если секрет не задан —
          # build.mjs не создаёт comtrade_key.php, работает public-эндпоинт.
          COMTRADE_KEY: ${{ secrets.COMTRADE_KEY }}

      # Выкладываем через lftp одной FTPS-сессией. Прежние варианты падали:
      # FTP-Deploy-Action обрывался на крупном main.js.dat, а curl-цикл
      # открывал новое соединение на каждый файл (хрупкий exit code, риск
      # rate-limit сервера). lftp mirror заливает всё за один логин.
      #
      # mirror без --delete не трогает config.php (хеш пароля создаётся на
      # сервере через setup.php, в dist не входит) — вход переживает выкладку.
      # Удаление ниже касается только точного списка устаревших ассетов
      # исключённого модуля; остальные серверные файлы не затрагиваются.
      - name: Deploy via lftp (FTPS)
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_SERVER_DIR: ${{ secrets.FTP_SERVER_DIR }}
        run: |
          set -e
          sudo apt-get update -qq && sudo apt-get install -y lftp
          lftp -c "
            set ssl:verify-certificate no;
            set ftp:ssl-force true;
            set ftp:ssl-protect-data true;
            set net:max-retries 2;
            set net:timeout 60;
            open -u '$FTP_USERNAME','$FTP_PASSWORD' '$FTP_SERVER';
            rm -f '$FTP_SERVER_DIR/assets/foreign_customs.js.dat';
            rm -f '$FTP_SERVER_DIR/assets/foreign_customs.js.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_hs_names.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_hs_names.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_co.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_co.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_pe.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_pe.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_kz.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_kz.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_kg.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_kg.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_kgs.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_kgs.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_kzs.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_kzs.dat.gz';
            rm -f '$FTP_SERVER_DIR/assets/fc_kzd.dat';
            rm -f '$FTP_SERVER_DIR/assets/fc_kzd.dat.gz';
            mirror -R --verbose --no-perms ./dist/ '$FTP_SERVER_DIR';
          "
          echo 'Deploy complete.'
